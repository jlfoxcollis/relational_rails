<head>
</head>
<body>
  <ul class="navbar">
    <li><%= link_to "Home Page", home_path, method: :get %></li>
    <li><%= link_to "Trucks Index", trucks_path, method: :get %></li>
    <li><%= link_to "Edit Truck", edit_truck_path(@truck.id), method: :get %></li>
    <li><%= link_to "Dealer Page", show_dealer_path(@truck.dealer_id), method: :get %></li>
    <li><%= link_to "Delete", destroy_truck_path(@truck.id), method: :delete %></li>
  </ul>
<table>
  <tr>
    <th>Year</th>
    <th>Make</th>
    <th>Model</th>
    <th>Dealer ID</th>
    <th>Dealer Name</th>
  </tr>
  <tr>
    <td><%= @truck.year %></td>
    <td><%= @truck.make %></td>
    <td><%= @truck.model %></td>
    <td><%= @truck.dealer_id %></td>
    <td><%= @truck.dealer.name %></td>
  </tr>
</table>
</body>
